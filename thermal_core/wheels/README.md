# Pre-built thermal_core wheels go here.
#
# File naming follows PEP 425:
#   thermal_core-<version>-<python_tag>-<abi_tag>-<platform_tag>.whl
#
# Wheels are NOT committed to git (see .gitignore).
# They are attached as GitHub Release assets instead.
# The install script downloads the correct wheel automatically.
