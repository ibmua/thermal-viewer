# Pre-built thermal_core wheels go here.
#
# File naming follows PEP 425:
#   thermal_core-<version>-<python_tag>-<abi_tag>-<platform_tag>.whl
#
# Small bundled wheels may be committed here for convenience.
# Full release artifacts are also attached to GitHub Releases.
# The install script first tries the local bundled wheel, then falls back to
# downloading the correct asset from the latest GitHub Release.
